apiVersion: v1
kind: Service
metadata:
  name: kafka-connect
  namespace: data
  labels:
    app.kubernetes.io/name: kafka-connect
spec:
  selector:
    app.kubernetes.io/name: kafka-connect
  ports:
    - name: http
      port: 8083
      targetPort: http
    - name: metrics
      port: 9404
      targetPort: metrics
