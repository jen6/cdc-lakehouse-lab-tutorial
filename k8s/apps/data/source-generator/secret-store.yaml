apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: source-generator-aws-secrets-manager
  namespace: data
spec:
  provider:
    aws:
      service: SecretsManager
      region: ap-northeast-2
      auth:
        jwt:
          serviceAccountRef:
            name: source-generator
